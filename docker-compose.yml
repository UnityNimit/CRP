services:
  api:
    build: ./backend
    ports:
      - "8080:8080"
    env_file:
      - .env
    environment:
      - SPRING_PROFILES_ACTIVE=local

  web:
    build: ./frontend
    ports:
      - "80:80"
    depends_on:
      - api
